<%= form_for(@mantenedora) do |f| %>
  <% if @mantenedora.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mantenedora.errors.count, "error") %> prohibited this mantenedora from being saved:</h2>

      <ul>
      <% @mantenedora.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
