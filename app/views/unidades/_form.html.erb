<%= form_for(@unidade) do |f| %>
  <% if @unidade.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unidade.errors.count, "error") %> prohibited this unidade from being saved:</h2>

      <ul>
      <% @unidade.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
